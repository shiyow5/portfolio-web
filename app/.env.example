# ===== Cloudflare Pages Functions (server-side only) =====
# Gemini API key — used by functions/api/chat.ts. NEVER expose to client.
GEMINI_API_KEY=""

# Cloudflare Turnstile secret (server-side verification).
TURNSTILE_SECRET=""

# ===== Client-exposed (prefix with VITE_) =====
# Turnstile site key — safe to ship to browser.
VITE_TURNSTILE_SITEKEY=""
